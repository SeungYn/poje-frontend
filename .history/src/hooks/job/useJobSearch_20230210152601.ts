export default function useJobSearch() {
  const prefetchJobSearch = (keyword: string) => {
    //Todo 검색결과 프리패치 함수
  };

  const getJobListByKeyword = (keyword: string) => {
    //Todo 검색결과 반환하기
  };
}
