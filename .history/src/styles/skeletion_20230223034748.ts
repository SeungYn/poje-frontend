import styled from 'styled-components';

export const EmptyDiv = styled.div`
  background: #ddd;
  border-radius: 1rem;
  overflow: hidden;
  position: relative;
  border-radius: 1rem;
  margin: 0 auto;
`;
